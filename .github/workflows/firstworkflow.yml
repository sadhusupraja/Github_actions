name: first workflow
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build step
        run: echo "build job"
      - name: hi step  
        run: echo "hi"
  deploy:
     needs: build
     runs-on: ubuntu-latest
     steps:
       - name: deploy step
         run: echo "deploy job"
